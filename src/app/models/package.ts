export class Package {
  package: string;
  status: string;
  priority: string;
  section: string;
  'installed-size': string;
  maintainer: string;
  architecture: string;
  source: string;
  version: string;
  replaces: string;
  provides: string;
  depends: string;
  suggests: string;
  breaks: string;
  conflicts: string;
  description: string;
  'original-maintainer': string;
}
